# AI → App handoff — {{PROJECT_NAME}}

## 1. Транспорт

- HTTPS к ноде
- `Authorization: Bearer <GPT_ACTIONS_BEARER_SECRET>` (эталон)

## 2. Заголовки

| Заголовок | Назначение |
|-----------|------------|
| `X-User-Id` | Псевдоним пользователя для push подписи |
| … | |

## 3. Границы

- GPT не выполняет on-chain вызовы напрямую
- Статусы — только по ответу API

## 4. Риски

- Утечка Bearer в логи клиента

## 5. Hooks

- Ротация секрета в OpenAI и в env ноды синхронно
