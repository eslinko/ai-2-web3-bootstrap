# Ingest boundary

Приём HTTP-запроса на crystallization, парсинг JSON, извлечение `upload_id`, `upload_token`, `signed_data_item`, `payload_size`.

**Controlled by:** `uploader.enabled`, `uploader.payload_contract_mode`
