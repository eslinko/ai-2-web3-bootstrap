# Payload contract boundary

Дополнительная доменная / структурная проверка поверх транспортных полей (если `uploader.payload_contract_mode` = `strict` или проект расширяет контракт).

**Controlled by:** `uploader.payload_contract_mode`
