# Contracts starter

- `hardhat.config.cjs` — сети и пути как в эталонном монорепо.
- `scripts/deploy_router.js` — точка входа `DEPLOY_ACTION`; расширяйте модулями.
- Положите контракты в `contracts/*.sol`; тесты в `test/`.

Генератор подставляет `{{project_slug}}` в `package.json`.
