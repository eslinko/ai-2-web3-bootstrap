# Router scaffold

- `deploy_router.stub.js` — минимальный entry: `registry` + `list` / `help`.
- Переименуй при переносе в целевой репо; подключай `require("../config")` когда появится реальная конфигурация.
- Граница: router **не** должен содержать всю бизнес-логику — выноси в модули.
