# Config scaffold

- `index.stub.js` — пробует `dotenv` для `scripts/.env` (путь из `env-path.stub.js`).
- `constants.stub.js` — плейсхолдеры имён сети/контрактов.
- Скопируй без суффикса `.stub` в целевом репо и обнови `require` пути.
