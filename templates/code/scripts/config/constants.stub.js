/**
 * Placeholder names — замени на реальные после интервью / деплоя.
 */
module.exports = {
  /** Example: primary router contract name for docs / logging */
  PRIMARY_ROUTER_NAME: "REPLACE_ME_Router",
  /** Hardhat network default for local runs */
  DEFAULT_NETWORK: "localhost",
};
