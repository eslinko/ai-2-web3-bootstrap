# Shell glue templates

- `sync-artifacts.template.sh` — задай `SOURCE` и `TARGET`.
- `smoke-health.template.sh` — `NODE_URL`, `UPLOADER_URL` через env.
- Убери суффикс `.template` в целевом репо; сделай `chmod +x` при необходимости.
