# GPT payload contracts

Контрактные структуры payload для handoff GPT -> backend.

## What lives here

- schema sections / type hints для `strict_json`;
- contract versioning placeholders;
- required vs optional fields markers.

## Controlled by decisions

- `gpt.output_structure_mode`
- `gpt.backend_handoff_mode`
