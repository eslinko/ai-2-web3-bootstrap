# Auth service boundaries

Сюда попадают сервисные границы для:

- wallet challenge/verify
- secrets source resolver (env/vault/hybrid)
- inter-service auth contract helpers

Управляющие решения:

- `security.wallet_auth_mode`
- `security.secrets_source`
