# Signing service boundaries

Эта папка описывает границы сервисов подписи:

- upload token signing (JWT)
- arweave data item signing boundary
- evm transaction signing boundary

Управляющие решения:

- `security.upload_token_mode`
- `security.arweave_signing_mode`
- `security.evm_signing_mode`
